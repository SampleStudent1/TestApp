<%= form_for(@ad_format) do |f| %>
  <% if @ad_format.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ad_format.errors.count, "error") %> prohibited this ad_format from being saved:</h2>

      <ul>
      <% @ad_format.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
